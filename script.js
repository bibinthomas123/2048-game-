import Grid from "./grid.js";

const gameBoard = document.getElementById("game-board");
const grid = new Grid(gameBoard);

grid.radomEmptyCell().tile = new Tile(gameBoard)
grid.radomEmptyCell().tile = new Tile(gameBoard)
